(function() {

    var Api = function(apiEvents) {
            this.events = apiEvents;
        },
        prototypeMethods = {

        };

    window.Api = Api;

    $.extend(Api.prototype, prototypeMethods);
}())